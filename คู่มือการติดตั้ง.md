# คู่มือติดตั้งระบบแจ้งซ่อม (Google Sheet + Apps Script)

ไฟล์ที่ได้รับ: `Code.gs`, `Form.html`, `Admin.html`

---

## ขั้นตอนที่ 1: สร้าง Google Sheet และเปิด Apps Script

1. ไปที่ [sheets.google.com](https://sheets.google.com) สร้างสเปรดชีตใหม่ ตั้งชื่อ เช่น "ฐานข้อมูลแจ้งซ่อม"
2. เมนู **ส่วนขยาย (Extensions) → Apps Script**
3. จะเปิดหน้าต่างแก้ไขโค้ด (ชื่อไฟล์เริ่มต้นคือ `Code.gs`)

## ขั้นตอนที่ 2: วางโค้ด

1. ลบโค้ดเดิมใน `Code.gs` แล้ววางเนื้อหาจากไฟล์ `Code.gs` ที่ให้มาทั้งหมด
2. คลิก **+** ข้าง Files → เลือก **HTML** → ตั้งชื่อไฟล์ว่า `Form` (ไม่ต้องใส่ .html) → วางเนื้อหาจาก `Form.html`
3. ทำซ้ำอีกครั้ง ตั้งชื่อไฟล์ว่า `Admin` → วางเนื้อหาจาก `Admin.html`
4. กด **บันทึก** (ไอคอนแผ่นดิสก์ หรือ Ctrl+S)

## ขั้นตอนที่ 3: สร้างชีตข้อมูลเริ่มต้น

1. ในแถบเมนูด้านบนของ Apps Script เลือกฟังก์ชัน `setupSpreadsheet` (dropdown ข้างปุ่ม Run/Debug)
2. กด **Run** ▶️
3. ครั้งแรกจะขอสิทธิ์การเข้าถึง (Authorize) → เลือกบัญชี Google ของคุณ → กด "Advanced" → "Go to (โปรเจกต์) (unsafe)" → Allow
   - ข้อความเตือน "unsafe" ปกติ เพราะเป็นสคริปต์ที่คุณเขียนเอง ไม่ใช่แอปจากบุคคลที่สาม
4. กลับไปที่ Google Sheet จะเห็น 4 ชีตใหม่: `Requests`, `Technicians`, `Categories`, `AdminConfig`
5. **สำคัญ:** ไปที่ชีต `AdminConfig` แก้ค่า `ADMIN_PIN` จาก `1234` เป็นรหัสของคุณเอง
6. ไปที่ชีต `Technicians` ใส่รายชื่อช่างจริงของคุณ (แถวตัวอย่างลบออกได้)
7. ไปที่ชีต `Categories` แก้ไข/เพิ่มประเภทงานซ่อมตามที่ใช้จริง

## ขั้นตอนที่ 4: ตั้งค่า LINE แจ้งเตือน (ไม่บังคับ)

> LINE Notify ปิดให้บริการไปแล้วตั้งแต่ 31 มี.ค. 2025 ระบบนี้ใช้ **LINE Messaging API** ผ่าน LINE Official Account แทน

1. ไปที่ [LINE Developers Console](https://developers.line.biz/console/) → สร้าง Provider และ **Messaging API Channel**
2. ในหน้า Channel → แท็บ **Messaging API** → เลื่อนลงหา "Channel access token" → กด **Issue** เพื่อสร้าง token (แบบ long-lived)
3. คัดลอก token นี้ไว้
4. หา **Group ID** ที่จะส่งข้อความเข้า — ใช้วิธีนี้ (Code.gs มีตัวช่วยดึงให้อัตโนมัติแล้ว):

   **4.1 วางโค้ดเวอร์ชันล่าสุด**
   - อัปเดต `Code.gs` ในโปรเจกต์ Apps Script ด้วยไฟล์ล่าสุดที่มีฟังก์ชัน `doPost` (ตัวรับ webhook ชั่วคราว)
   - กด **Deploy → Manage deployments** → เลือก deployment "Public Form" → ไอคอนดินสอ → Version: **New version** → Deploy
   - คัดลอก URL ของ deployment นี้ไว้ (เช่น `https://script.google.com/macros/s/AKfycb.../exec`)

   **4.2 ตั้งค่า Webhook ใน LINE Console**
   - ไปที่ LINE Developers Console → Channel ของคุณ → แท็บ **Messaging API**
   - ช่อง **Webhook URL** → วาง URL จากข้อ 4.1 → กด **Update**
   - กด **Verify** เพื่อทดสอบ (ควรขึ้นสำเร็จ ✅ ถ้าไม่สำเร็จ ให้เช็คว่า deploy ใหม่แล้วหรือยัง)
   - เปิดสวิตช์ **Use webhook** เป็น ON
   - ปิดสวิตช์ **Auto-reply messages** ใน [OA Manager](https://manager.line.biz/) → Settings → Response settings (กันไม่ให้บอทตอบข้อความอัตโนมัติกวนกลุ่ม)

   **4.3 เชิญบอทเข้ากลุ่มช่าง**
   - เปิดแอป LINE → สแกน QR code ของ Official Account (หน้า Basic settings ใน Console) → เพิ่มเป็นเพื่อน
   - สร้างกลุ่ม LINE สำหรับช่าง (หรือใช้กลุ่มเดิม) → เชิญ Official Account เข้ากลุ่มด้วย
   - พิมพ์ข้อความอะไรก็ได้ในกลุ่ม 1 ข้อความ (เช่น "test")

   **4.4 ดึง Group ID จากชีต**
   - กลับไปที่ Google Sheet ของคุณ → จะมีชีตใหม่ชื่อ **`LineWebhookLog`** ปรากฏขึ้น
   - จะเห็นแถวที่มีคอลัมน์ "Group ID (ใช้อันนี้)" พร้อมค่า ID ยาวๆ ขึ้นต้นด้วย `C` (เช่น `C1234567890abcdef...`)
   - คัดลอกค่านี้ไปใส่เป็น `LINE_TARGET_ID` ใน Script Properties

   **4.5 ปิด Webhook (ทำหรือไม่ทำก็ได้)**
   - เนื่องจากระบบนี้แค่ "ส่ง" แจ้งเตือนออกไปทางเดียว ไม่จำเป็นต้อง "รับ" ข้อความกลับ
   - ถ้าอยากปิด กลับไปที่ Messaging API tab → ปิดสวิตช์ **Use webhook** เป็น OFF ได้เลย (ไม่กระทบการส่งแจ้งเตือน)
   - ถ้าจะส่งหาช่างคนเดียวแทนกลุ่ม ให้ทัก 1:1 กับ Official Account แทนการพิมพ์ในกลุ่ม จะได้แถว "User ID (แชทเดี่ยว)" แทน ใช้ค่านั้นเป็น `LINE_TARGET_ID` ได้เหมือนกัน
5. กลับมาที่ Apps Script → เมนู **⚙️ Project Settings** (ไอคอนเฟือง) → เลื่อนลง **Script Properties** → Add script property:
   - `LINE_CHANNEL_ACCESS_TOKEN` = token ที่คัดลอกมา
   - `LINE_TARGET_ID` = Group ID หรือ User ID ที่จะส่งแจ้งเตือน

ถ้ายังไม่พร้อมตั้งค่า LINE ระบบยังทำงานได้ปกติ แค่จะไม่มีการแจ้งเตือนเข้า LINE เท่านั้น (ดูงานใหม่ได้ใน Dashboard)

## ขั้นตอนที่ 5: Deploy เป็น 2 Web App (ฟอร์ม + แอดมิน แยกสิทธิ์กัน)

### Deployment ที่ 1 — ฟอร์มแจ้งซ่อม (เปิดสาธารณะ)

1. มุมขวาบน กด **Deploy → New deployment**
2. เลือกประเภท (ไอคอนเฟือง) → **Web app**
3. ตั้งค่า:
   - Description: `Public Form`
   - Execute as: **Me**
   - Who has access: **Anyone**
4. กด **Deploy** → คัดลอก URL ที่ได้ (นี่คือลิงก์ฟอร์มแจ้งซ่อม ใช้ค่า default `?page=form` อยู่แล้ว)
5. แชร์ URL นี้ให้พนักงาน/ลูกค้าใช้แจ้งซ่อม (ทำ QR Code ติดหน้างานได้)

### Deployment ที่ 2 — หน้าแอดมิน (จำกัดสิทธิ์)

1. กด **Deploy → New deployment** อีกครั้ง
2. เลือก **Web app**
3. ตั้งค่า:
   - Description: `Admin Dashboard`
   - Execute as: **Me**
   - Who has access:
     - ถ้าองค์กรใช้ **Google Workspace**: เลือก "Anyone within [ชื่อองค์กร]" (ปลอดภัยสุด — ต้อง login ด้วยอีเมลองค์กรก่อนถึงจะเห็นหน้า PIN)
     - ถ้าไม่มี Google Workspace: เลือก "Anyone" แล้วพึ่ง PIN เป็นด่านป้องกัน (แนะนำให้ตั้ง PIN ที่คาดเดายาก และเปลี่ยนเป็นระยะ)
4. กด **Deploy** → คัดลอก URL ที่ได้
5. เพิ่ม `?page=admin` ต่อท้าย URL เช่น `https://script.google.com/macros/s/xxxxx/exec?page=admin`
6. เก็บลิงก์นี้ไว้เฉพาะทีมงาน/แอดมิน (ไม่ต้องแชร์สาธารณะ)

> หมายเหตุ: ทุกครั้งที่แก้โค้ดใน `Code.gs`/`Form.html`/`Admin.html` ต้องกด **Deploy → Manage deployments → แก้ไข (ไอคอนดินสอ) → Version: New version → Deploy** ใหม่ ไม่งั้น URL เดิมจะยังใช้โค้ดเวอร์ชันเก่าอยู่

## ขั้นตอนที่ 6 (ไม่บังคับ): ซ่อนแถบเตือนสีเทาของ Google

เมื่อเปิดลิงก์ `/exec` ตรงๆ Google จะแปะแถบเทาบอกว่า "แอปพลิเคชันนี้สร้างโดยผู้ใช้ Google Apps Script" ไว้บนสุดเสมอ (เป็นข้อความมาตรฐานของ Google เอง แก้ผ่านโค้ดไม่ได้) เลือกทำวิธีใดวิธีหนึ่งด้านล่างเพื่อซ่อน:

### ตัวเลือก A: ฝังผ่าน Google Sites (ง่ายสุด ไม่ต้องเขียนโค้ด)

1. ไปที่ [sites.google.com](https://sites.google.com) → **สร้าง (+)** เว็บไซต์ใหม่
2. ตั้งชื่อเว็บไซต์ เช่น "แจ้งซ่อม W&W"
3. คลิกที่พื้นที่ว่างในหน้า → แถบเครื่องมือด้านขวาเลือก **Embed (ฝัง)**
4. เลือกแท็บ **By URL** → วาง URL ของ Web App "Public Form" (ลงท้าย `/exec`) → กด **Insert**
5. ลากขอบกล่อง embed ให้เต็มความกว้าง/ความสูงของหน้า (หรือใช้เมนู Layout เลือกแบบเต็มจอ)
6. ไปที่ **Settings → Header type** เลือก **None** เพื่อซ่อนแถบหัวเว็บของ Google Sites เอง (ให้ดูเหมือนเว็บแอปจริงๆ)
7. มุมขวาบน กด **Publish** → ตั้งค่า URL ของเว็บไซต์ (เช่น `sites.google.com/view/wwrepair`)
8. ทำซ้ำอีกหน้าสำหรับแอดมิน (สร้างหน้าใหม่ในเว็บไซต์เดียวกัน → embed URL ที่มี `?page=admin` ต่อท้าย) แล้วตั้งเป็นหน้าที่ไม่โชว์ใน navigation menu (คลิกขวาที่หน้านั้น → Hide from navigation) เพื่อไม่ให้คนทั่วไปเห็นลิงก์แอดมินง่ายๆ

> ข้อดี: ฟรี, ไม่ต้องมีที่โฮสต์เอง, ทำ URL สวยได้ในระดับหนึ่ง
> ข้อจำกัด: URL ยังเป็นโดเมน `sites.google.com` อยู่ ถ้าต้องการโดเมนของตัวเองเช่น `repair.บริษัทคุณ.com` ต้องผูก custom domain เพิ่ม (Google Sites รองรับ แต่ต้องมีโดเมนของตัวเองก่อน)

### ตัวเลือก B: ใช้ไฟล์ HTML ที่ให้มา (`iframe-form.html`, `iframe-admin.html`)

ไฟล์ทั้งสองนี้เป็นหน้าเว็บเปล่าๆ ที่ฝัง `/exec` ไว้ใน iframe พร้อมหน้า loading สวยงาม ใช้ตอนที่คุณมีที่โฮสต์เว็บของตัวเอง (หรือใช้บริการฟรีอย่าง GitHub Pages / Cloudflare Pages / Netlify)

1. เปิดไฟล์ `iframe-form.html` ด้วยโปรแกรมแก้ไขข้อความ
2. หาบรรทัด `src="https://script.google.com/macros/s/แก้เป็น_DEPLOYMENT_ID_ของคุณ/exec"` → แก้เป็น URL จริงของ deployment "Public Form"
3. ทำแบบเดียวกันกับ `iframe-admin.html` แต่ใช้ URL ของ deployment "Admin Dashboard" (มี `?page=admin` ต่อท้ายอยู่แล้วในไฟล์)
4. อัปโหลดไฟล์ทั้งสองขึ้นบริการโฮสต์ฟรีที่เลือก เช่น:
   - **GitHub Pages**: สร้าง repo → อัปโหลดไฟล์ → เปิดใช้ Pages ใน Settings → ได้ URL แบบ `username.github.io/repo/iframe-form.html`
   - **Netlify Drop**: ลากไฟล์วางที่ [app.netlify.com/drop](https://app.netlify.com/drop) ได้ URL ทันทีโดยไม่ต้องสมัครบัญชี

> ข้อดี: คุมหน้าตา URL และดีไซน์ได้เต็มที่ ผูกโดเมนของตัวเองได้ง่ายกว่า
> ข้อจำกัด: ต้องมีที่โฮสต์ไฟล์ (แต่ตัวเลือกฟรีที่แนะนำก็เพียงพอสำหรับงานนี้)

## การใช้งานประจำวัน

- **ผู้แจ้ง**: เปิดลิงก์ฟอร์ม → กรอกข้อมูล → ได้เลขที่ตั๋ว (Ticket ID) กลับมา
- **แอดมิน/ช่าง**: เปิดลิงก์แอดมิน → ใส่ PIN → ดู Dashboard สรุปงาน หรือแท็บ "รายการงาน" เพื่อเปลี่ยนสถานะ/มอบหมายช่าง
- ข้อมูลทั้งหมดอยู่ในชีต `Requests` แก้ไขตรงในชีตได้เช่นกันหากจำเป็น

## ข้อจำกัดที่ควรทราบ

- Apps Script มี quota การรันต่อวัน (สำหรับบัญชีฟรีประมาณหลายพันครั้ง/วัน) เพียงพอสำหรับองค์กรขนาดเล็ก-กลาง
- หากมีคนแจ้งซ่อมพร้อมกันมากๆ ในเวลาเดียวกันบ่อยครั้ง (หลักร้อยพร้อมกัน) ควรพิจารณาย้ายไปใช้ฐานข้อมูลจริง (เช่น Firebase, PostgreSQL) ในอนาคต
- PIN เป็นการป้องกันแบบเบื้องต้น ไม่ใช่ระบบ authentication เต็มรูปแบบ — หากข้อมูลอ่อนไหวมาก แนะนำ deploy แบบจำกัดเฉพาะ Google Workspace องค์กร
